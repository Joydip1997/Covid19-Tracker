<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/home_menu"
        android:icon="@drawable/ic_home"
        android:title="Home"
        app:cnb_iconColor="@color/colorCardGreen"/>
    <item
        android:id="@+id/india_menu"
        android:icon="@drawable/ic_global"
        android:title="India"
        app:cnb_iconColor="@color/colorCardBlue"/>

    <item
        android:id="@+id/global_menu"
        android:icon="@drawable/ic_bacteria"
        android:title="Global"
        app:cnb_iconColor="@color/colorCardRed" />

</menu>